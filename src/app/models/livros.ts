export interface livros {
    id?: number;
    titulo:string;
    imagem: string;
    descricao:string;
    
}
// aqui declaramos os atributos (caracteristicas) do livros. A interrogação em frente do ID é para torna-lo  um campo não-obrigatório. COmo ele será gerado automaticamente, aqui não será preenchido manualmente no código.//